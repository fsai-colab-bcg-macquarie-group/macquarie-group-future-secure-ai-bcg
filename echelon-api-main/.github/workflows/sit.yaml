# This is a basic workflow to help you get started with Actions
name: CI Deployment to SIT

# Controls when the action will run. Invokes the workflow on push events but only for the main branch
on:
  push:
    branches: [ stg ]

# Permission can be added at job level or workflow level    
permissions:
  id-token: write   # This is required for requesting the JWT
  contents: read    # This is required for actions/checkout

jobs:
  echelon-coverage-test:
    uses: Future-Secure-AI/cicd-templates/.github/workflows/coverage-test-backend.yaml@feature/coverage
    secrets: inherit
    with:
      VERSION_NODE: "20"
      ENVIRONMENT: "sit"
  echelon-api-build:
    uses: Future-Secure-AI/cicd-templates/.github/workflows/docker-build.yaml@main
    secrets: inherit
    with:
      AWS_ACCOUNT: "050752608492"
      ENVIRONMENT: "sit"
  helm-deploy:
    needs: [echelon-api-build]
    uses: Future-Secure-AI/cicd-templates/.github/workflows/helm-deploy.yaml@main
    with:
      AWS_ACCOUNT: "050752608492"
      ENVIRONMENT: "sit"
      HELM_CHART_PATH: "./charts/echelon-api"
      NAMESPACE: "platform"
      HELM_RELEASE_NAME: "echelon-api"
      USE_IMAGE_OVERRIDE: true
      